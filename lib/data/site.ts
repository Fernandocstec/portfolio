/** Update these URLs before publishing. */
export const siteLinks = {
  github: "#",
  linkedin: "#",
  email: "mailto:hello@fernandocorrea.dev",
} as const;

export const site = {
  name: "Fernando Correa",
} as const;

/** Same order as `projects.items` in messages (en/pt). */
export const projectRepoHref = "#" as const;
